# Test Cases

Generated by JBS IntelliQE — 18 test case(s).

| Module | Cases |
|---|---|
| Authentication | 18 |

## Authentication

### TC-001 — Verify successful login with valid credentials redirects to Dashboard
**Feature:** User Login · **Priority:** P0 · **Type:** positive · **Severity:** Critical

Confirms a registered user can authenticate with valid credentials and is redirected to the Dashboard with a session established. This is the application gatekeeper flow; failure blocks all authorized HR activities.

**Precondition:** The registered user account with username 'Admin' and password 'admin123' exists and is active. The login page is reachable and no active session exists in the browser.

**Steps:**

1. Navigate to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login → Expected: The login page loads displaying the OrangeHRM logo, the Username field, the Password field, the 'Login' button, and the 'Forgot your password?' link
2. Enter 'Admin' into the field labelled 'Username' → Expected: The Username field shows 'Admin' with no validation error
3. Enter 'admin123' into the field labelled 'Password' → Expected: The Password field shows masked characters with no validation error
4. Click the 'Login' button → Expected: The user is authenticated and redirected to the Dashboard page (URL contains '/dashboard/index') within 3 seconds; the Dashboard heading is displayed

**Expected Result:** The user is authenticated, a valid session is created, and the Dashboard page is displayed.

**Tags:** smoke, regression, auth

### TC-002 — Verify login fails with valid username and invalid password
**Feature:** User Login · **Priority:** P0 · **Type:** negative · **Severity:** Critical

Confirms authentication is rejected when a correct username is paired with a wrong password and that the user remains on the login page with an 'Invalid credentials' error. Mitigates the risk of unauthorized access via incorrect credentials.

**Precondition:** The registered user account with username 'Admin' exists. No active session exists in the browser.

**Steps:**

1. Navigate to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login → Expected: The login page is displayed with Username and Password fields and the 'Login' button
2. Enter 'Admin' into the Username field → Expected: The Username field shows 'Admin'
3. Enter 'WrongPass999' into the Password field → Expected: The Password field shows masked characters
4. Click the 'Login' button → Expected: An 'Invalid credentials' error message is displayed and the user remains on the login page; no redirect to Dashboard occurs

**Expected Result:** The system rejects authentication, displays 'Invalid credentials', and keeps the user on the login page.

**Tags:** regression, auth

### TC-003 — Verify login fails with invalid username and valid password
**Feature:** User Login · **Priority:** P1 · **Type:** negative · **Severity:** Major

Confirms authentication is rejected when an unknown username is paired with a valid password and the generic 'Invalid credentials' message is shown (no username-enumeration hint). Mitigates unauthorized access and information disclosure.

**Precondition:** No account exists for username 'NotARealUser'. No active session exists in the browser.

**Steps:**

1. Navigate to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login → Expected: The login page is displayed
2. Enter 'NotARealUser' into the Username field → Expected: The Username field shows 'NotARealUser'
3. Enter 'admin123' into the Password field → Expected: The Password field shows masked characters
4. Click the 'Login' button → Expected: An 'Invalid credentials' error message is displayed and the user remains on the login page

**Expected Result:** The system rejects authentication with a generic 'Invalid credentials' message and no redirect occurs.

**Tags:** regression, auth

### TC-004 — Verify 'Required' messages appear for both fields when submitting an empty form
**Feature:** Field Validation · **Priority:** P1 · **Type:** negative · **Severity:** Major

Confirms field-level 'Required' validation blocks authentication when both the Username and Password fields are empty on submit. Mitigates weak validation and poor user experience.

**Precondition:** The login page is loaded with both the Username and Password fields empty.

**Steps:**

1. Navigate to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login → Expected: The login page is displayed with empty Username and Password fields
2. Click the 'Login' button without entering any values → Expected: A 'Required' validation message is displayed beneath the Username field and a 'Required' validation message is displayed beneath the Password field
3. Observe the page URL and state → Expected: The user remains on the login page and no authentication request proceeds

**Expected Result:** Both fields display 'Required' and authentication is blocked.

**Tags:** regression, auth, data-validation

### TC-005 — Verify 'Required' message appears when only the Username field is blank
**Feature:** Field Validation · **Priority:** P1 · **Type:** negative · **Severity:** Moderate

Confirms that leaving the Username blank while entering a password displays 'Required' for Username only and blocks authentication. Distinguishes field-specific validation behavior.

**Precondition:** The login page is loaded. The Username field is empty and a value is available for the Password field.

**Steps:**

1. Navigate to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login → Expected: The login page is displayed
2. Leave the Username field empty and enter 'admin123' into the Password field → Expected: The Password field shows masked characters and the Username field remains empty
3. Click the 'Login' button → Expected: A 'Required' validation message is displayed beneath the Username field only; no error appears for Password and the user remains on the login page

**Expected Result:** Username displays 'Required', Password shows no error, and authentication is blocked.

**Tags:** regression, auth, data-validation

### TC-006 — Verify 'Required' message appears when only the Password field is blank
**Feature:** Field Validation · **Priority:** P1 · **Type:** negative · **Severity:** Moderate

Confirms that leaving the Password blank while entering a username displays 'Required' for Password only and blocks authentication. Distinguishes field-specific validation behavior.

**Precondition:** The login page is loaded. A value is available for the Username field and the Password field is empty.

**Steps:**

1. Navigate to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login → Expected: The login page is displayed
2. Enter 'Admin' into the Username field and leave the Password field empty → Expected: The Username field shows 'Admin' and the Password field remains empty
3. Click the 'Login' button → Expected: A 'Required' validation message is displayed beneath the Password field only; no error appears for Username and the user remains on the login page

**Expected Result:** Password displays 'Required', Username shows no error, and authentication is blocked.

**Tags:** regression, auth, data-validation

### TC-007 — Verify whitespace-only username resolves to blank after trim and displays 'Required'
**Feature:** Field Validation · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Confirms a username consisting only of spaces is trimmed to empty and triggers the 'Required' validation rather than being submitted. Covers the trimming data rule combined with mandatory-field validation.

**Precondition:** The login page is loaded. The Username field will receive spaces only.

**Steps:**

1. Navigate to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login → Expected: The login page is displayed
2. Enter five space characters into the Username field → Expected: The Username field visually contains only spaces
3. Enter 'admin123' into the Password field → Expected: The Password field shows masked characters
4. Click the 'Login' button → Expected: The whitespace username is trimmed to empty and a 'Required' validation message is displayed beneath the Username field; authentication does not proceed

**Expected Result:** The whitespace-only username is treated as blank, 'Required' is shown, and authentication is blocked.

**Tags:** regression, auth, data-validation

### TC-008 — Verify username with leading and trailing spaces is trimmed before authentication
**Feature:** Field Validation · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Confirms a valid username padded with leading and trailing spaces is trimmed and successfully authenticated, so accidental padding does not cause false login failures. Covers the username-trim data rule.

**Precondition:** The registered user 'Admin' with password 'admin123' exists. No active session exists in the browser.

**Steps:**

1. Navigate to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login → Expected: The login page is displayed
2. Enter '  Admin  ' (with two leading and two trailing spaces) into the Username field → Expected: The Username field shows the padded value
3. Enter 'admin123' into the Password field → Expected: The Password field shows masked characters
4. Click the 'Login' button → Expected: The Username is trimmed to 'Admin', the user is authenticated, and the Dashboard page is displayed

**Expected Result:** Leading/trailing spaces are trimmed and login succeeds, redirecting to the Dashboard.

**Tags:** regression, auth, data-validation

### TC-009 — Verify password characters are masked on screen during entry
**Feature:** Password Masking · **Priority:** P1 · **Type:** security · **Severity:** Major

Confirms the Password field masks typed characters so credentials are not visible on screen. Mitigates the credential-disclosure risk from exposed password text.

**Precondition:** The login page is loaded with the Password field visible.

**Steps:**

1. Navigate to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login → Expected: The login page is displayed
2. Enter 'admin123' into the Password field → Expected: The Password field displays masked characters (dots/asterisks) instead of the plain text 'admin123'
3. Inspect the Password field element's type attribute using browser DevTools → Expected: The input element has type='password', confirming masking is enforced at the field level

**Expected Result:** The password is rendered masked on screen and the input type is 'password'.

**Tags:** regression, security, ui

### TC-010 — Verify pressing Enter with valid credentials triggers login equivalent to clicking Login
**Feature:** Keyboard Submission · **Priority:** P2 · **Type:** positive · **Severity:** Moderate

Confirms the Enter key submits the login form and authenticates the user identically to clicking the Login button. Validates the keyboard-submission convenience flow.

**Precondition:** The registered user 'Admin' with password 'admin123' exists. No active session exists in the browser.

**Steps:**

1. Navigate to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login → Expected: The login page is displayed
2. Enter 'Admin' into the Username field → Expected: The Username field shows 'Admin'
3. Enter 'admin123' into the Password field → Expected: The Password field shows masked characters
4. Press the Enter key while focus is in the Password field → Expected: The login process is triggered, the user is authenticated, and the Dashboard page is displayed within 3 seconds

**Expected Result:** Pressing Enter submits the form and authenticates the user, matching the Login button behavior.

**Tags:** regression, auth, accessibility

### TC-011 — Verify 'Forgot your password?' link navigates to the Password Reset page
**Feature:** Forgot Password · **Priority:** P2 · **Type:** positive · **Severity:** Minor

Confirms clicking the 'Forgot your password?' link redirects the user to the Password Reset page to begin recovery. Validates the navigation-only scope of this feature.

**Precondition:** The login page is loaded and the 'Forgot your password?' link is visible.

**Steps:**

1. Navigate to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login → Expected: The login page is displayed with the 'Forgot your password?' link visible
2. Click the 'Forgot your password?' link → Expected: The user is redirected to the Password Reset / Reset Password page (URL contains '/requestPasswordResetCode') and the reset request form is displayed

**Expected Result:** The user is navigated to the Password Reset page.

**Tags:** regression, ui

### TC-012 — Verify authenticated session persists when pressing browser Back after login
**Feature:** Session Management · **Priority:** P0 · **Type:** security · **Severity:** Critical

Confirms that after a successful login, pressing the browser Back button does not return the user to a usable login form but keeps the session valid and redirects back to the Dashboard, preventing a re-login loop. Mitigates the High session-management risk.

**Precondition:** The registered user 'Admin' with password 'admin123' exists. The user has just logged in successfully and is on the Dashboard.

**Steps:**

1. Navigate to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login and log in with 'Admin' / 'admin123' → Expected: The user is authenticated and the Dashboard page is displayed
2. Click the browser Back button → Expected: The user is not presented with a usable login form; the session remains valid
3. Observe the resulting page → Expected: The user is redirected to the Dashboard (URL contains '/dashboard/index') and is not prompted to log in again

**Expected Result:** The session remains valid and the user is kept on the Dashboard rather than re-entering the login flow.

**Tags:** regression, security, auth

### TC-013 — Verify direct access to a protected Dashboard URL after logout redirects to login
**Feature:** Session Management · **Priority:** P0 · **Type:** security · **Severity:** Critical

Confirms that once a user logs out, directly requesting a protected page URL redirects to the login page and denies access, ensuring session invalidation. Mitigates unauthorized access to protected HR pages.

**Precondition:** The registered user 'Admin' has logged in and then logged out, so no active session exists.

**Steps:**

1. Log in with 'Admin' / 'admin123', then click the user dropdown and select 'Logout' → Expected: The user is logged out and returned to the login page
2. Enter the protected URL https://opensource-demo.orangehrmlive.com/web/index.php/dashboard/index directly in the browser address bar and load it → Expected: The application detects no valid session and redirects to the login page
3. Observe the resulting page → Expected: The login page is displayed (URL contains '/auth/login') and the Dashboard content is not shown

**Expected Result:** Access to the protected page is denied and the user is redirected to the login page.

**Tags:** regression, security, auth

### TC-014 — Verify username at maximum boundary length of 100 characters is accepted by the field
**Feature:** Field Validation · **Priority:** P2 · **Type:** data · **Severity:** Moderate

Confirms the Username field accepts a value at its documented 100-character maximum without truncation or client-side rejection, validating the upper boundary of the length data rule.

**Precondition:** The login page is loaded. A 100-character username value is prepared for entry.

**Steps:**

1. Navigate to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login → Expected: The login page is displayed
2. Enter the 100-character 'a' string into the Username field → Expected: The Username field accepts and displays all 100 characters without truncation
3. Enter 'admin123' into the Password field → Expected: The Password field shows masked characters
4. Click the 'Login' button → Expected: The form submits with the full 100-character username; since no matching account exists, an 'Invalid credentials' error is displayed rather than a length/truncation error

**Expected Result:** The Username field accepts the full 100-character boundary value and processes submission without truncation.

**Tags:** regression, data-validation

### TC-015 — Verify username entry beyond 100 characters is truncated or rejected consistently
**Feature:** Field Validation · **Priority:** P2 · **Type:** data · **Severity:** Moderate

Confirms overflow handling when a username longer than the 100-character maximum is entered, ensuring the input is either truncated to 100 or rejected consistently rather than silently accepting an over-length value. Covers the overflow data rule not addressed by the exact-100 boundary case.

**Precondition:** The login page is loaded. A 150-character username value is prepared for entry.

**Steps:**

1. Navigate to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login → Expected: The login page is displayed
2. Enter the 150-character 'b' string into the Username field → Expected: The Username field either caps input at 100 characters or shows a length validation error; it does not silently store all 150 characters as a valid value
3. Enter 'admin123' into the Password field and click the 'Login' button → Expected: The submission is handled consistently with the enforced maximum (over-length input truncated to 100 or rejected); no server error or unhandled exception occurs and the user remains on the login page

**Expected Result:** Over-length username input is truncated to 100 characters or rejected consistently, with no silent acceptance of over-length data.

**Tags:** regression, data-validation

### TC-016 — Verify SQL/script injection strings in the Username field are sanitized and rejected
**Feature:** Field Validation · **Priority:** P1 · **Type:** security · **Severity:** Critical

Confirms that injection payloads entered into the Username field do not bypass authentication or trigger script execution, and are treated as ordinary invalid input. Mitigates SQL Injection and XSS security risks explicitly listed in the requirements.

**Precondition:** The login page is loaded. No active session exists. The system is expected to sanitize input and prevent SQL Injection and XSS.

**Steps:**

1. Navigate to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login → Expected: The login page is displayed
2. Enter the string ' OR '1'='1 into the Username field → Expected: The Username field accepts the raw text without executing or interpreting it
3. Enter the string <script>alert(1)</script> into the Password field → Expected: The Password field masks the input and no script executes / no alert dialog appears
4. Click the 'Login' button → Expected: Authentication is not bypassed; an 'Invalid credentials' error is displayed, no SQL error is exposed, no script alert fires, and the user remains on the login page

**Expected Result:** Injection payloads are sanitized and rejected as invalid credentials with no authentication bypass and no script execution.

**Tags:** regression, security, auth

### TC-017 — Verify credentials are transmitted over HTTPS and never appear in the URL
**Feature:** User Login · **Priority:** P1 · **Type:** security · **Severity:** Critical

Confirms login credentials are sent over an encrypted HTTPS connection via the request body and are never exposed as GET query parameters or in the URL. Mitigates credential-in-transit disclosure risk.

**Precondition:** The login page is loaded over HTTPS. Browser DevTools Network tab (or a proxy such as Burp) is available to inspect the login request.

**Steps:**

1. Open browser DevTools, select the Network tab, and navigate to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login → Expected: The page loads over HTTPS and the connection is shown as secure
2. Enter 'Admin' into the Username field and 'admin123' into the Password field, then click the 'Login' button → Expected: A login request is captured in the Network tab
3. Inspect the captured login request method, URL, and payload → Expected: The request uses HTTPS and the POST method; the username and password are carried in the request body, not in the URL query string
4. Inspect the final Dashboard URL and browser address bar → Expected: No credential values appear anywhere in the URL or query parameters

**Expected Result:** Credentials are transmitted over HTTPS in the request body and never exposed in the URL or query parameters.

**Tags:** regression, security, auth

### TC-018 — Verify account lockout or throttling engages after repeated failed login attempts
**Feature:** Session Management · **Priority:** P1 · **Type:** security · **Severity:** Major

Confirms the application enforces brute-force protection by locking or throttling the account after multiple consecutive failed login attempts, as required by the security NFR. Mitigates automated password-guessing attacks.

**Precondition:** The registered user 'Admin' exists. No active session exists. Brute-force protection (lockout or throttling) is expected per the security NFR.

**Steps:**

1. Navigate to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login → Expected: The login page is displayed
2. Submit the login form with username 'Admin' and password 'wrongPassword0' five consecutive times → Expected: Each attempt returns an 'Invalid credentials' error
3. Attempt a sixth login submission with the same invalid credentials → Expected: The system engages protection: the account is temporarily locked, a CAPTCHA/throttling delay is enforced, or a lockout message is displayed, rather than continuing to accept unlimited attempts
4. Attempt to log in with the correct password 'admin123' while lockout/throttling is active → Expected: Login is blocked or delayed per the protection policy until the lockout/throttle window expires, confirming brute-force protection is active

**Expected Result:** After repeated failed attempts the system enforces lockout or throttling rather than allowing unlimited login attempts.

**Tags:** regression, security, auth
