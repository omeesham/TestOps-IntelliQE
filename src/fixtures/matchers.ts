/**
 * Custom expect matchers.
 *
 * Placeholder for project-specific assertions. Add via `expect.extend({...})`
 * and import this file once from a fixture or setup so the matchers register.
 * Empty by default — no custom matchers are required to run the suite.
 */
export {};
